export enum FormFieldType {
    NUMBER = 'number',
    TEXTAREA = 'textarea',
    SELECT = 'select',
    CHECKBOX = 'checkbox',
    AUTOCOMPLETE = 'autocomplete',
    DATETIME = 'datetime',
    RADIO = 'radio',
    TEXT = 'text',
    PASSWORD = 'password',
}
